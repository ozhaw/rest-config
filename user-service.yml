eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
default:
  security:
    includes: /user/api/user/operations/*
